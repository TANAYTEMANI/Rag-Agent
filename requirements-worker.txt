# Docling's dependencies — installed after torch is already present.
# We install docling itself with --no-deps to prevent pip re-resolving torch.
# This list is docling 2.33.0's full deps minus torch/torchvision (pre-installed).
docling-core>=2.20,<3
docling-ibm-models>=3.4,<4
docling-parse>=4.0,<5
deepsearch-glm>=0.26,<1
easyocr>=1.7,<2
huggingface-hub>=0.23,<1
pypdfium2>=4,<5
python-docx>=1.1,<2
python-pptx>=1.0,<2
scipy>=1.13,<2
transformers>=4.46,<5
Pillow>=10
# Missing optional deps flagged during build
beautifulsoup4>=4.12,<5
filetype>=1.2
marko>=2.1,<3
pluggy>=1.0,<2
pylatexenc>=2.10,<3
lxml>=4.0,<6
# click and typer pinned to versions compatible with both celery and docling
click>=8.1,<8.2
typer>=0.12,<0.16
